language: python3
notifications: {
  "email": false,
  "irc": {
    "channels": ["ircs://irc.spi.gt:6697/#MiniDigger"],
    "on_success": "change",
    "on_failure": "always",
    "skip_join": true
  }
}
scripts: "chmod +x scripts/build.sh && scripts/build.sh"
deploy:
  provider: script
  script: "chmod +x scripts/deploy-build-results.sh && scripts/deploy-build-results.sh"
  on:
    branch: master
skip_cleanup: true
